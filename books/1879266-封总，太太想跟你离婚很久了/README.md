# 封总，太太想跟你离婚很久了

- 书籍 ID：`1879266`
- 数据库：`../../data/novel-chapters.sqlite`
- 建书日期：2026-07-09

## 当前阶段

统计于 2026-09-01（以数据库为准，后续以本书 README 更新）：

- 空壳登记：已建书和索引组，未导入章节（0 章，导入状态 `idle`）
- 已建空索引组：`charactersrelationships`（人物关系索引）、`relationships`（任务关系索引），均无章节状态和事实

## 下一步

- 继续使用：先通过 `/library` 导入章节，再按需构建索引
- 确认不再需要：按数据与批量任务规则第 4 条完成可恢复备份后，经人工确认清理

## 使用规则

索引提示词当前仅存于数据库（`book_index_prompts` 表），未落地 `inputs/prompts/` 文件
