# 死亡就变强：全球诡异求我别送

- 书籍 ID：`8720253`
- 数据库：`../../data/novel-chapters.sqlite`
- 题材：都市诡异复苏+无限流系统流，人类与有名号星级诡异双线出场，系统面板为权威数据源，作者 PS 与玩梗为噪声（详见提示词说明）

## 当前阶段

统计于 2026-09-01（以数据库为准，后续以本书 README 更新）：

- 章节已导入 376/376
- L1 路由索引未构建
- `characters`（角色形象）索引组尚未在数据库中创建，L2 未开始；提示词已定稿

## 下一步

在 `/prompts` 页面创建 `characters` 索引组并粘贴定稿提示词（只取两条 `---` 分隔线之间的正文），启动 L2 构建

## 当前入口

- L2 索引提示词定稿：`inputs/prompts/character-image-l2-index.md`

## 使用规则

提示词以 `inputs/prompts/` 内定稿为准；在 `/prompts` 页面临时调整且验证有效后，必须回写对应文件
